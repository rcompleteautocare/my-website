{
  "name": "rcac-messaging",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SMS-first / voice-fallback customer messaging for R Complete Auto Care (RingCentral + Tekmetric).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "reminders": "node src/jobs/dayBeforeReminders.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
