import { Plus,Search,SlidersHorizontal,MoreHorizontal } from "lucide-react"; import { Badge } from "@/components/ui/badge"; import { Button } from "@/components/ui/button"; import { Card } from "@/components/ui/card"; import { Input } from "@/components/ui/input";
export function SectionPage({title,description,columns,rows,action=`New ${title.replace(/s$/,'')}`,empty}:{title:string;description:string;columns:string[];rows:string[][];action?:string;empty?:boolean}){return <div className="mx-auto max-w-[1500px] space-y-5"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><h2 className="text-2xl font-semibold tracking-tight text-white">{title}</h2><p className="!m-0 mt-1 text-sm text-zinc-500">{description}</p></div><Button><Plus size={16}/>{action}</Button></div><Card><div className="flex flex-col gap-3 border-b border-white/[.06] p-4 sm:flex-row"><div className="relative max-w-sm flex-1"><Search className="absolute left-3 top-3 text-zinc-600" size={16}/><Input className="h-10 pl-9" placeholder={`Search ${title.toLowerCase()}…`}/></div><Button variant="outline"><SlidersHorizontal size={15}/>Filters</Button></div>{empty?<div className="grid min-h-72 place-items-center p-8 text-center"><div><div className="mx-auto mb-3 grid size-11 place-items-center rounded-xl bg-white/[.05]"><Search className="text-zinc-600"/></div><h3 className="text-sm text-white">No records found</h3><p className="!m-0 mt-1 text-xs text-zinc-600">Try adjusting your search or create the first record.</p></div></div>:<div className="overflow-x-auto"><table className="w-full min-w-[800px] text-left text-xs"><thead className="border-b border-white/[.06] bg-white/[.015] text-[10px] uppercase tracking-wider text-zinc-600"><tr>{columns.map(c=><th key={c} className="px-5 py-3 font-semibold">{c}</th>)}<th/></tr></thead><tbody>{rows.map((row,i)=><tr key={i} className="border-b border-white/[.05] transition hover:bg-white/[.025]">{row.map((cell,j)=><td key={j} className={`px-5 py-4 ${j===0?"font-semibold text-zinc-200":"text-zinc-500"}`}>{j===columns.findIndex(x=>x==="Status")?<Badge className={cell.includes("Ready")||cell.includes("Active")||cell.includes("Completed")?"text-emerald-400":cell.includes("Waiting")?"text-amber-400":"text-blue-400"}>{cell}</Badge>:cell}</td>)}<td><Button variant="ghost" size="icon"><MoreHorizontal size={16}/></Button></td></tr>)}</tbody></table></div>}</Card></div>}
