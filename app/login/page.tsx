"use client"; import { useState } from "react"; import { signIn } from "next-auth/react"; import { useRouter } from "next/navigation"; import { Command,Eye,EyeOff,LoaderCircle,LockKeyhole,Mail } from "lucide-react"; import { Input } from "@/components/ui/input"; import { Button } from "@/components/ui/button";
export default function Login(){const router=useRouter();const [show,setShow]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const fd=new FormData(e.currentTarget);const r=await signIn("credentials",{email:fd.get("email"),password:fd.get("password"),redirect:false});setBusy(false);if(r?.error)setError("Email or password is incorrect.");else{router.push("/command-center");router.refresh()}}return <div className="fixed inset-0 z-[100] grid min-h-screen bg-[#0b0c0d] text-zinc-100 lg:grid-cols-2"><section className="relative hidden overflow-hidden border-r border-white/[.06] bg-[#111213] p-12 lg:flex lg:flex-col"><div className="absolute inset-0 bg-[radial-gradient(circle_at_30%_25%,rgba(249,115,22,.18),transparent_35%)]"/><div className="relative flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-orange-500"><Command/></span><div><b>R Complete</b><span className="block text-[10px] uppercase tracking-[.2em] text-zinc-500">Command Center</span></div></div><div className="relative my-auto max-w-lg"><p className="text-xs font-semibold uppercase tracking-[.2em] text-orange-400">One shop. Complete visibility.</p><h1 className="mt-5 text-5xl font-semibold leading-[1.08] tracking-[-.04em] text-white">Run your entire operation from one place.</h1><p className="mt-6 max-w-md text-base leading-7 text-zinc-500">Sales, repair orders, customers, vehicles, calls, and profitability—working together in real time.</p><div className="mt-10 grid grid-cols-3 gap-3">{[["$284k","Monthly sales"],["31","Cars this month"],["46.5%","Gross margin"]].map(([v,l])=><div key={l} className="rounded-2xl border border-white/[.07] bg-white/[.03] p-4"><b className="block text-xl">{v}</b><span className="text-[10px] text-zinc-600">{l}</span></div>)}</div></div><p className="relative text-[10px] text-zinc-700">© 2026 R Complete Auto Care</p></section><section className="flex items-center justify-center p-6"><div className="w-full max-w-sm"><div className="mb-9 lg:hidden"><span className="grid size-10 place-items-center rounded-xl bg-orange-500"><Command/></span></div><p className="text-xs font-semibold uppercase tracking-[.18em] text-orange-400">Welcome back</p><h2 className="mt-3 text-3xl font-semibold tracking-tight text-white">Sign in to your account</h2><p className="!m-0 mt-2 text-sm text-zinc-500">Use your team credentials to continue.</p><form className="mt-8 space-y-5" onSubmit={submit}><label className="block"><span className="mb-2 block text-xs font-medium text-zinc-400">Email address</span><div className="relative"><Mail className="absolute left-3.5 top-3.5 text-zinc-600" size={16}/><Input name="email" type="email" defaultValue="owner@rcomplete.com" className="pl-10" required/></div></label><label className="block"><span className="mb-2 block text-xs font-medium text-zinc-400">Password</span><div className="relative"><LockKeyhole className="absolute left-3.5 top-3.5 text-zinc-600" size={16}/><Input name="password" type={show?"text":"password"} defaultValue="ChangeMe123!" className="px-10" required/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3.5 top-3 text-zinc-600 hover:text-white">{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label>{error&&<p role="alert" className="!m-0 rounded-xl border border-red-500/20 bg-red-500/10 p-3 text-xs text-red-400">{error}</p>}<Button className="w-full" disabled={busy}>{busy?<LoaderCircle className="animate-spin" size={17}/>:null}{busy?"Signing in…":"Sign in"}</Button></form><p className="!m-0 mt-6 text-center text-[10px] text-zinc-700">Protected by encrypted session security</p></div></section></div>}
