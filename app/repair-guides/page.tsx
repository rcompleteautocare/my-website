import type { Metadata } from "next";
import Link from "next/link";
import { repairGuides } from "@/lib/repair-guides";
export const metadata: Metadata = { title: "Vehicle Repair Guides | Crown Point Auto Repair", description: "Diagnostic-first vehicle repair guides for common Honda, Ford, Chevrolet, Nissan, BMW, and Jeep problems from R Complete Auto Care.", alternates: { canonical: "/repair-guides" } };
export default function RepairGuides() { return <main style={{ maxWidth: 1050, margin: "0 auto", padding: "72px 24px" }}><p style={{ color: "#c92f3b", fontWeight: 800 }}>R COMPLETE AUTO CARE RESOURCE LIBRARY</p><h1 style={{ fontSize: "clamp(40px, 6vw, 64px)", marginBottom: 18 }}>Vehicle-Specific Repair Guides</h1><p style={{ maxWidth: 760, fontSize: 19, lineHeight: 1.75 }}>Learn what common symptoms can mean, how technicians separate similar causes, when driving may be unsafe, and why testing matters before parts are replaced.</p><div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(280px, 1fr))", gap: 18, marginTop: 42 }}>{Object.entries(repairGuides).map(([slug, g]) => <Link href={`/repair-guides/${slug}`} key={slug} style={{ border: "1px solid #ddd", borderRadius: 12, padding: 26, textDecoration: "none", color: "#222" }}><strong style={{ fontSize: 21 }}>{g.title}</strong><p style={{ lineHeight: 1.6, color: "#5d5d5d" }}>Symptoms, causes, diagnostic steps, driving guidance, and next steps.</p><span style={{ color: "#c92f3b", fontWeight: 800 }}>Read guide →</span></Link>)}</div><p style={{ marginTop: 42 }}>For a vehicle-specific answer, <Link href="/book">book a diagnostic appointment</Link> or review <Link href="/services">all services</Link>.</p></main> }
