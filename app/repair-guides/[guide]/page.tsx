import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { repairGuides, type RepairGuideSlug } from "@/lib/repair-guides";
export function generateStaticParams() { return Object.keys(repairGuides).map(guide => ({ guide })); }
export async function generateMetadata({ params }: { params: Promise<{ guide: string }> }): Promise<Metadata> { const { guide } = await params; const g = repairGuides[guide as RepairGuideSlug]; if (!g) return {}; return { title: `${g.title} | Crown Point, IN`, description: `${g.title}: symptoms, common causes, diagnostic process, driving guidance, and repair decisions from a Crown Point diagnostic-first shop.`, alternates: { canonical: `/repair-guides/${guide}` } }; }
export default async function RepairGuide({ params }: { params: Promise<{ guide: string }> }) {
  const { guide } = await params; const g = repairGuides[guide as RepairGuideSlug]; if (!g) notFound(); const url = `https://www.rcompleteautocare.com/repair-guides/${guide}`;
  const driving = guide.includes("coolant") ? "Do not keep driving if the engine is overheating, misfiring severely, or losing coolant rapidly. Shut it off and arrange a tow to reduce the risk of engine damage." : guide.includes("cvt") ? "Stop driving if the vehicle cannot accelerate normally, displays an overheating warning, slips severely, or cannot engage reliably. Continuing can increase damage and create a traffic hazard." : "Stop and seek guidance for low oil pressure, overheating, smoke, severe noise, a flashing warning light, or a major loss of power. Mild symptoms still deserve prompt diagnosis.";
  const graph = { "@context": "https://schema.org", "@graph": [{ "@type": "Article", headline: g.title, mainEntityOfPage: url, author: { "@id": "https://www.rcompleteautocare.com/#business" }, publisher: { "@id": "https://www.rcompleteautocare.com/#business" }, about: [{ "@type": "Thing", name: g.vehicle }, { "@type": "Thing", name: g.system }] }, { "@type": "BreadcrumbList", itemListElement: [{ "@type": "ListItem", position: 1, name: "Home", item: "https://www.rcompleteautocare.com" }, { "@type": "ListItem", position: 2, name: "Repair Guides", item: "https://www.rcompleteautocare.com/repair-guides" }, { "@type": "ListItem", position: 3, name: g.title, item: url }] }] };
  return <article style={{ maxWidth: 850, margin: "0 auto", padding: "72px 24px", color: "#222" }}><p style={{ color: "#c92f3b", fontWeight: 800 }}>VEHICLE REPAIR GUIDE · CROWN POINT, INDIANA</p><h1 style={{ fontSize: "clamp(40px, 6vw, 60px)", lineHeight: 1.08 }}>{g.title}</h1><p className="speakable" style={{ fontSize: 20, lineHeight: 1.75 }}>A {g.system} concern on a {g.vehicle} can have several causes that produce similar symptoms. Testing the complete system before replacing parts protects against an incomplete or unnecessary repair.</p><h2>What are the common symptoms?</h2><p style={{ lineHeight: 1.8 }}>Drivers may notice {g.symptoms}. The symptom’s timing, temperature, load, warning messages, and recent repair history all help shape an efficient test plan.</p><h2>Why does it happen?</h2><p style={{ lineHeight: 1.8 }}>Possible causes include {g.causes}. These are possibilities, not a diagnosis; the correct repair depends on measurements from the specific vehicle.</p><h2>How is it diagnosed?</h2><p style={{ lineHeight: 1.8 }}>Our process may include {g.tests}. We confirm the failed system and look for the underlying reason it failed before presenting priorities and an estimate.</p><h2>Can I keep driving?</h2><p style={{ lineHeight: 1.8 }}>{driving}</p><h2>How much does repair usually cost?</h2><p style={{ lineHeight: 1.8 }}>Cost varies with the confirmed cause, vehicle configuration, related damage, parts choice, labor access, and current parts pricing. A broad internet estimate cannot replace a vehicle-specific diagnosis. R Complete Auto Care provides a transparent estimate and performs no repair without customer approval.</p><h2>Why choose R Complete Auto Care?</h2><p style={{ lineHeight: 1.8 }}>Crown Point and Northwest Indiana drivers receive diagnostic-first testing, experienced ASE-certified technicians, clear priorities, and documented recommendations. Same-day diagnostics may be available depending on schedule and complexity.</p><p style={{ marginTop: 40 }}><Link href={g.service}>View the related repair service</Link> · <Link href="/inspection-checklist">See our inspection process</Link> · <Link href="/book">Book an appointment</Link> · <Link href="/contact">Contact us</Link></p><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(graph).replace(/</g, "\\u003c") }} /></article>;
}
